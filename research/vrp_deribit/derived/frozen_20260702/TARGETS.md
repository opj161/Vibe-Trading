# Frozen parity targets — 2026-07-02

Regenerated by rerunning `data_prep.py`, `phase4_trend_expression_options.py`,
`phase4b_hybrid_and_stress.py`, `phase5_sol_expression.py` from the present raw
parquets (`data/parquet/btc_option_trades_deribit.parquet`,
`data/parquet/sol_usdc_option_trades_deribit.parquet`). Full stdout captured in
`data_prep.log`, `phase4.log`, `phase4b.log`, `phase5.log` in this directory.
Derived CSVs (`daily_index.csv`, `atm_iv_30d.csv`, `term_structure.csv`,
`phase4_per_run.csv`, `phase5_sol_per_run.csv`) are frozen copies of the
regenerated files. These are the Nautilus parity-project's acceptance targets
(§81/§82 narrative values in `vibe_trading_research_findings.md` are the
sanity cross-check — see reconciliation below).

## Gate 0 reconciliation vs §81/§82

**BTC (phase4/phase4b, §81)**: SPOT ann=+15.5% sharpe=0.91 maxDD=-18.3%;
OPT-DELTA ann=+20.4% sharpe=0.84 maxDD=-29.1%; OPT-BUDGET ann=+18.7%
sharpe=0.91 maxDD=-21.9%; **H1 S/OB ann=+18.2% sharpe=1.00 maxDD=-21.0%**.

**SOL (phase5, §82.3 table)** — exact match:

| Track | Total P&L | Ann. | Sharpe | MaxDD |
|---|---:|---:|---:|---:|
| SPOT | +$209,225 | 9.0% | 0.37 | -35.7% |
| OPT-DELTA | +$49,935 | 2.3% | 0.30 | -35.8% |
| OPT-BUDGET | +$158,548 | 7.2% | 0.42 | -22.1% |
| **H1 S/OB** | **+$262,160** | **11.7%** | **0.54** | **-21.7%** |

At 2x spread: H1 S/OB* +$237,730 / 10.7% / 0.51 / -21.9% — matches §82.3's
"+$238k / 0.51" to rounding. **Reconciles exactly — no drift.**

## Denominators (the 46-vs-53 mechanism, both stated explicitly)

- **SOL raw direction runs in tape window**: 53 (20 long, 33 short) —
  `sol_direction_runs.csv` rows whose `[start,end]` falls inside the SOL tape
  (2024-03-11 → 2026-07-02).
- **SOL `phase5_sol_per_run.csv` rows**: 46 (47 lines incl. header). The gap
  (53 → 46 = 7 runs) is the `<2-daily-dates` skip in `run_tracks()`
  (`phase5_sol_expression.py:185-186`) — a run whose start/end fall on the
  same or adjacent single daily-index date never reaches `per_run`, but *is*
  still counted in the "53 in-window" narrative denominator.
- **SOL option-entry-skipped runs**: 14 (long=6, short=8) — of the 46 (or the
  relevant in-window subset per script), no option print found within
  `[anchor, anchor+3d]` matching type/DTE/moneyness. This is a *different*
  skip mechanism from the `<2-dates` skip above (an option-chain-liquidity
  skip, not a run-duration skip) and applies on top of it.
- **BTC raw direction runs**: 110 (`btc_direction_runs.csv`, 111 lines incl.
  header). **BTC `phase4_per_run.csv` rows**: 95 (96 lines incl. header);
  stdout reports `runs=95 option-entry-skipped=2` — i.e. phase4 counts
  "option-entry-skipped" over the 95 post-<2-dates-skip runs (using
  `found_any` semantics per CLAUDE.md's documented phase4-vs-phase5 skip
  quirk), not over the raw 110.

## Skip-accounting quirk (reproduce, do not "fix")

- `phase4` (BTC) sets `found_any` only after a *rolled* leg or when a
  track's total P&L != 0 (`phase4:200-206`).
- `phase5` (SOL) sets `entry_found` immediately on any entry
  (`phase5:205`).

The Nautilus parity harness (Phase 2, `leg_planner.py`) must reproduce both
skip mechanisms *per script* — do not unify them into one rule.
